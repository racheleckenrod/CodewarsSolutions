// Code as fast as you can! You need to double the integer 
// and return it.
// Forgot to set the (i * 2) equal to i so that it updates, on
// the first try

function doubleInteger(i) {
    // i will be an integer. Double it and return it.
    i = (i * 2)
    return i;
  }
