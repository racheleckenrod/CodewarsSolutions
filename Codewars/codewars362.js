// The kata has been preloaded with a function called blackBox. Figure out what the function does.

// You may need to think laterally!


console.log(blackBox.toString(JSopenSesame()))

// just calling JSopenSesame() worked to open the box.. the toString() method  gave us a message in the testing area